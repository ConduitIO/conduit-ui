# conduit-ui
Conduit's built-in web UI — observe + operate (live record flow, per-stage inspection, pipeline graph, start/stop). Config-as-code stays the source of truth; the UI does not author pipelines.
